name: Build and Push YAML with Docker
on: [push]

jobs:
  build-and-push:
  runs-on: ubuntu-latest
  steps:
  - uses: actions/checkout@v2
  - name: Build and push YAML
uses: docker/build-push-action@v2
  with:
  context: .
  push: true
  tags: username/repo:latest
